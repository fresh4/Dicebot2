var Enmap = require('enmap');
var fs = require('fs');
exports.run = (bot, msg, args) => {
    //Case 1: !help
    //Case 2: !help <command>

    //Import require all the command files via loop
    //Order commands by category 
    //Return the name (usage) description as an embedded message
}